# Triage

A TypeScript-based Slack agent backend for GitHub triage, built for OSS maintainers.

## Getting Started

1. Copy `.env.local.example` to `.env.local` and fill in your credentials.
2. Install dependencies:

```bash
npm install
```

3. Run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) in your browser.

## Project Structure

See [CONTEXT.md](./CONTEXT.md) for architecture details.
