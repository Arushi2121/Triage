# types

Shared TypeScript type definitions.

Domain types, API request/response shapes, and cross-cutting interfaces used across core, adapters, integrations, and formatters. Keeps the type contract in one place.
