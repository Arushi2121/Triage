# core/duplicates

Platform-agnostic duplicate detection logic.

Identifies likely duplicate issues by comparing titles, bodies, labels, and metadata against existing issues. Returns confidence scores and match candidates without calling external APIs directly.
