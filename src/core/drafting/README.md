# core/drafting

Platform-agnostic response and action drafting logic.

Generates suggested replies, labels, assignees, and next-step actions for maintainers. Output is structured domain data — formatting for Slack or GitHub happens in the formatters layer.
