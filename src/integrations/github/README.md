# integrations/github

GitHub API and webhook integration.

Authenticates via GitHub App credentials, fetches issues/PRs/comments, applies labels, and processes incoming webhooks. Translates GitHub API responses into domain types consumed by core logic.
