# integrations/mcp

Model Context Protocol (MCP) integration.

Connects to MCP servers for extended tooling (e.g. codebase search, documentation lookup). Used when triage workflows need context beyond what GitHub and Slack provide directly.
