# formatters/slack

Slack-specific message formatting.

Converts domain objects (triage results, draft responses, notifications) into Slack Block Kit payloads, plain text, and thread replies. No business logic — presentation only.
